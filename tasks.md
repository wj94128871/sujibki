# 작업 분해 (tasks.md) — 시장조사·분석 대시보드

- **작성팀**: 기획팀 (planning-team) · **모드**: standard
- **기준**: plan.md(FR/AC), trd.md(기술), userflow.md(여정)
- **사용 스킬 로그**: `tasks`(에픽→스토리→태스크 분해, 의존성·DoD), `classify-size`(S/M/L), `spec-kit`(tasks 템플릿)
- **규모 표기**: S(≤0.5일) / M(0.5~2일) / L(2~5일). 태스크는 독립적·atomic·리뷰 가능 단위.

> 크롤러 5종은 이미 코드 완성·테스트 통과 상태(`crawler_status_인계.md`) → **수행 재실행·환경 격리·적재 배선** 위주로 태스크를 잡는다. 분석·대시보드·API가 핵심 신규 개발.

---

## Epic 1 — 수집·적재 파이프라인 (FR-01~04)

### Story 1.1 크롤러 실행 환경 격리·통합 (trd §3.1, 인계서 주의)
| 태스크 | 규모 | 설 | DoD |
|---|---|---|---|
| T1.1.1 venv A/B 구성(각 크롤러 별도 인터프리터) 문서화·스크립트화 | S | 크롤러 5종 실행 스크립트, venv 명시 | 5종이 각자 venv에서 실행됨 |
| T1.1.2 5종 크롤러 재실행 검증(샘플) | S | 5종 실행·미리보기 | 샘플 산출물 생성 확인 |

### Story 1.2 표준 스키마 정제·멱등 적재 (FR-03)
| 태스크 | 규모 | DoD |
|---|---|---|
| T1.2.1 공통 안건 스키마 정의(projects) DDL + 마이그레이션 | M | Neon 스키마 반영·마이그레이션 버전 |
| T1.2.2 예산·카테고리·날짜 정규화 맵 구현 | M | 단위 통일·null 정책 적용 |
| T1.2.3 멱등 upsert(원본URL·source 복합키) 구현 | M | 중복 적재 0, 재실행 안전 |
| T1.2.4 `client_id` 제외·개인정보 필터(프리모아) | S | 저장 데이터에 이메일 미포함 |

### Story 1.3 수집 이력·러닝 (FR-02, FR-11)
| 태스크 | 규모 | DoD |
|---|---|---|
| T1.3.1 collection_runs 스키마 + 적재/기록 로직 | S | 수집 시각·건수·상태 기록 |
| T1.3.2 MVP 수동 트리거 CLI(wrapper) | S | 5종 일괄 실행·이력 기록 |
| T1.3.3 월간 자동 스케줄(Phase2) 설계 골격 | S | Cron/런너 설계서(구현은 Phase2) |

**Epic 1 DoD**: 5종 데이터가 Neon에 멱등 적재, 수집 이력 기록, 개인정보 제외.

---

## Epic 2 — 분석 파이프라인 (FR-06~08)

### Story 2.1 집계·분석 지표 산출 (FR-06)
| 태스크 | 규모 | DoD |
|---|---|---|
| T2.1.1 카테고리 수요 점유율 집계(precompute 테이블) | M | 카테고리별 건수·점유율 산출 |
| T2.1.2 예산/기간 분포 집계 | M | 버킷 히스토그램(null 제외+카운트) |
| T2.1.3 상주vs도급 비중 집계 | S | 유형별 비중 |
| T2.1.4 기술 키워드 랭킹·월간 변화 집계 | M | 키워드 빈도·증감 |
| T2.1.5 월간 신규 등록 추이 집계 | M | 월별 시계열 |

### Story 2.2 기능 개선 방향 분석 (FR-07)
| 태스크 | 규모 | DoD |
|---|---|---|
| T2.2.1 공급 동향(지원자 수·레벨) 집계 | M | 수요·공급 신호 산출 |
| T2.2.2 기능 개선 후보 규칙 템플릿 구현 | M | 근거 기반 후보 목록 산출 |

### Story 2.3 시장성 인사이트 도출 (FR-08)
| 태스크 | 규모 | DoD |
|---|---|---|
| T2.3.1 고성장 카테고리/키워드 산출(성장률) | M | 증가율 계산·주석 |
| T2.3.2 신뢰도 주석 체계 구현(높음/중간/낮음) | S | 저신뢰 수치 주석 표기 |
| T2.3.3 분석 결과 DB(analysis_*.) 적재 | S | 집계 테이블 적재 |

**Epic 2 DoD**: 모든 분석 지표·기능방향·시장성 인사이트가 계산되어 DB에 적재.

---

## Epic 3 — API 계층 (trd §3.4, FR-04/05/06/07/08/10/11)

### Story 3.1 대시보드 집계 API
| 태스크 | 규모 | DoD |
|---|---|---|
| T3.1.1 Worker 스캐폴딩 + Neon 커넥션(읽기 계정) | M | Worker→Neon 연결, 보안 |
| T3.1.2 `/api/summary` 구현 | S | 요약 카드 데이터 |
| T3.1.3 `/api/analysis` 구현 | M | 5개 분석 차트 데이터 |
| T3.1.4 `/api/insights` 구현 | M | 기능방향·시장성 인사이트 |

### Story 3.2 데이터 조회 API
| 태스크 | 규모 | DoD |
|---|---|---|
| T3.2.1 `/api/sources` 구현 | S | 소스별 요약·추이 |
| T3.2.2 `/api/projects` 목록·검색·페이징 | M | 소스 필터·검색·페이징 |
| T3.2.3 `/api/projects/:id` 경량 상세 | S | DB 필드 상세 |
| T3.2.4 `/api/runs` 수집 이력 | S | 수집 상태 |

### Story 3.3 캐시·성능
| 태스크 | 규모 | DoD |
|---|---|---|
| T3.3.1 집계 캐시(수집 후 무효화) | S | 캐시 헤더·무효화 구현 |

**Epic 3 DoD**: 모든 API 정상 응답·캐시·보안(읽기 계정), 결과 JSON 규격 일관.

---

## Epic 4 — 대시보드 UI (FR-09, userflow)

### Story 4.1 단일 페이지 대시보드
| 태스크 | 규모 | DoD |
|---|---|---|
| T4.1.1 Pages SPA 스캐폴딩 + 레이아웃 | M | 헤더·섹션 구조 |
| T4.1.2 헤더 요약 카드 컴포넌트 | S | 총건수·소스별·최근 수집 |
| T4.1.3 소스별 현황 섹션 | M | UJ-2 목록·필터 |
| T4.1.4 분석 차트(카테고리·예산·기간·상주·키워드·추이) | L | UJ-3, 차트 반응형 |
| T4.1.5 기능 개선 방향·시장성 인사이트 섹션 | M | UJ-4, 신뢰 뱃지 |
| T4.1.6 수집 이력 섹션 | S | UJ-1/F-11 |

### Story 4.2 상태·빈/에러 처리
| 태스크 | 규모 | DoD |
|---|---|---|
| T4.2.1 빈 상태·에러·로딩 처리 | S | 4.1 각 섹션에 적용 |
| T4.2.2 반응형(모바일/데스크톱) · 다크모드 | M | 프레임워크 테마 적용 |

**Epic 4 DoD**: 단일 페이지에서 6개 섹션 확인 가능, 반응형·디자인(design-system) 연동.

---

## Epic 5 — 배포·검증 (NFR-03/05)

### Story 5.1 배포·환경
| 태스크 | 규모 | DoD |
|---|---|---|
| T5.1.1 Cloudflare Pages · Workers 배포 파이프라인 | M | 배포 링크 접속 가능 |
| T5.1.2 Neon 마이그레이션 배포·시드 데이터 | S | 스키마 배포·샘플 적재 |
| T5.1.3 Secrets(DB DSN) 관리 | S | 코드에 평문 DSN 없음 |

### Story 5.2 검증
| 태스크 | 규모 | DoD |
|---|---|---|
| T5.2.1 크롤러 실행·적재 통합 검증 | M | 5종 적재 확인·이력 기록 |
| T5.2.2 대시보드 E2E 검증(개발QA 협조) | M | 수용 기준 충족 |

**Epic 5 DoD**: 배포 링크로 로그인 없이 접근, 전 기능 동작.

---

## 의존성 그래프 (요약)

```
Epic1(수집·적재) ──▶ Epic2(분석) ──▶ Epic3(API) ──▶ Epic4(UI)
   ▲                                    │
   └──────── (적재 선행) ───────────────┘
Epic1 → Epic2 → Epic3 → Epic4 (직렬) + Epic5(배포)는 병렬/후행
```
- Epic1 선행 필수(적재 없이는 분석·API·UI 무의미). Epic5 배포는 Epic3·4 후반에 병렬 가능.
- 크롤러는 선행 완성품 → **재실행·배선** 위주라 초기 스프린트에 빠르게 적재(Demo 가능).

## 우선순위/실행 순서 (개발팀 참고)
1. Epic1(수집·적재) — 데이터 선행
2. Epic2(분석) — 핵심 가치
3. Epic3(API) — 데이터 노출
4. Epic4(UI) — 한눈에 보기
5. Epic5(배포·검증) — 상시 후행

*끝 — 개발팀이 tech-design.md에서 구체화 후 TDD로 구현한다.*
