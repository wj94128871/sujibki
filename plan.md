# 기획서 (PRD) — 시장조사·분석 대시보드 (plan.md)

- **작성팀**: 기획팀 (planning-team)
- **작업 규모 모드**: standard (9단계 파이프라인 — 4단계 기획)
- **기준 문서**: `requirements.md` (재작성하지 않음, 본 문서에서 구체화), `research-report.md`, `crawler_status_인계.md`
- **산출 파일**: plan.md / trd.md / userflow.md / tasks.md / test-design.md (+ 각 .html)

---

## 0. 문서 목적 · 사용 방법

- 본 plan.md는 요구사항(requirements.md)을 **구체화**한 PRD(제품 요구사항)다. 요구사항문서의 내용을 변경하지 않으며, 기능 우선순위·수용 기준(AC)·MVP 범위·리스크를 확정한다.
- 기술 설계는 `trd.md`(기술 요구사항), 사용자 여정은 `userflow.md`, 개발 실행 단위는 `tasks.md`, 검증은 `test-design.md`가 담당한다.
- **사용 스킬 로그**: `prd-writer`(Given/When/Then AC + Edge Case 구조), `spec-kit`(명세 우선·plan/tasks 템플릿), `feature-prioritization`(M/S/C 우선순위·RICE 감안), `scope-cutting`(MVP 범위 슬라이싱), `classify-size`(태스크 S/M/L 규모 판정, tasks.md), `data-model`(공통 안건 스키마, trd.md), `management-consultant`(시장성 분석 지표), `plan-tests`(AC→테스트 매핑 구조, test-design.md), `md-to-html`(.html 생성).

---

## 1. 제품 개요 (Overview)

### 1.1 한 줄 정의
> SI(외주/프리랜서) 매칭 사이트(위시켓·프리모아), 스타트업 공모(모두의창업 u300), 해커톤(Devpost)의 **안건 데이터를 주기적으로 크롤링·표준화 저장**하고, 이를 바탕으로 **시장조사 지표·기능 개선 방향·시장성 인사이트를 한눈에 보는 단일 웹 대시보드**로 제공하는 시스템.

### 1.2 문제 / 가치
- **문제**: 외주·프리랜서·스타트업·해커톤 수요는 여러 출처에 흩어져 있고, "어떤 카테고리/기술/예산이 커지고 있는가"를 수치로 판단하기 어렵다. 각 사이트 UI가 달라 직접 비교·취합이 번거롭다.
- **가치(차별화)**: 여러 출처 안건을 **표준 스키마로 통합·정규화**해 카테고리·예산·기간·지역·기술 키워드 분포와 **월간 추이(어느 영역이 커지는지)**를 수치로 제시 → "플랫폼/사업을 열면 어느 영역이 시장성이 있는가"의 근거를 제공(성공기준 SC-2).

### 1.3 타깃 사용자 · 접근
- 주 사용자: 요청 팀(팀 단위, 개인/관리자 1명 이상 열람), 비개발·개발자 모두가 직관적으로 읽는 시각화 요약 선호.
- 접근: **로그인/권한 없이** 웹 URL로 접근 (NG5·NFR-03).

---

## 2. 목표 / 비목표

### 2.1 목표 (Goals, requirements §2.1 유지·구체화)
| ID | 목표 | 구체화 |
|---|---|---|
| G1 | 월 단위 자동 수집 | 위시켓·프리모아(도급), u300(현재+1기), Devpost(해커톤) **5종 크롤러**로 안건 수집 |
| G2 | 정제·표준화·저장 | 공통 안건 스키마로 정규화(단위 통일), 중복 제거, Neon(PostgreSQL) 저장 |
| G3 | 시장조사 분석 | 카테고리 수요 점유율, 예산/기간 분포, 상주vs도급, 기술 키워드, 월간 등록 추이 |
| G4 | 기능 개선 방향 | 수집 안건·공급 동향(지원자 수·레벨) 기반 기능 추가/개선 후보 도출 |
| G5 | 시장성 인사이트 | 고성장 카테고리/키워드(예: B2B AI 구축) 도출 |
| G6 | 한눈에 보기 | 단일 대시보드 페이지로 시각화(차트·카드·표·추이) |

### 2.2 비목표 (Non-Goals, requirements §2.2 유지)
- NG1 입찰/계약/매칭 자동화 ❌ · NG2 외주사/개발자 계정·프로필·메신저 ❌ · NG3 실시간(분/초) 수집 ❌ · NG4 수집 대상 외 사이트 무분별 크롤링 ❌ · NG5 로그인/권한/멀티테넌시 ❌
- **추가 비목표(본 기획)**: NG6 프리모아 `client_id`(이메일)·개별 파트너 프로필(`/m7`) 등 개인정보는 수집·저장하지 않는다(리서치 발견 §3.1, NFR-06). NG7 위시켓 상세의 로그인 게이트 항목(모집 요건·근무 환경 내역)을 MVP에서 크롤링하지 않는다(목록 필드 원천 권장, OQ-2).

---

## 3. 범위

### 3.1 데이터 소스 (MVP 확정) — OQ-1 귀결
> 크롤러 5종이 이미 준비/완성되어 있음(`crawler_status_인계.md`). MVP는 아래 4개 출처·5종 데이터셋을 모두 수집한다.

| 출처 | 데이터셋 | 크롤러 | 상태 | 수집 규모(알려진 값) | 스키마 그룹 |
|---|---|---|---|---|---|
| 위시켓 (도급) | 프로젝트(외주/기간제) 안건 | `crawl_wishket_oauth.py` | ✅ 코드 완성·테스트 통과 | 모집중 표기 약 6.6만 / MVP 1회 수집 | SI 도급 |
| 프리모아 (도급) | 프로젝트(도급·기간제·상주) 안건 | `crawl_freemoa.py` | ✅ 코드 완성·테스트 통과 | 누적 약 2.3만 | SI 도급 |
| u300 모두의창업 (현재 2기) | 스타트업 팀 상세(트랙·지역·사업 아이디어) | `crawl_u300.py` | ✅ **실제 수집 완료** | 현재 357건 | 스타트업 공모 |
| u300 1기(과거 전시) | 1기 팀(peNo) | `crawl_u300.py` | ✅ **실제 수집 완료** | 60건 | 스타트업 공모 |
| Devpost (해커톤) | 해커톤 프로젝트(소프트웨어·스택) | `crawl_devpost.py` | ✅ 코드 완성·테스트 통과 | 목록 24+ 상세(샘플) | 해커톤 |

- **Phase 2 확장 후보(크롤러 미구축)**: 크몽, Freemo, CyanNest, 아임잡 (robots 허용 확인됨. 원티드긱스는 봇 차단으로 제외 — 리서치 §3.1).

### 3.2 개인정보·법적 수집 제한 — OQ-2 귀결
- **위시켓**: 목록 페이지 비로그인 필드만 수집(제목·예산·기간·카테고리·레벨·지역·등록일·마감·지원자). 상세의 로그인 항목 제외. `crawl-delay 5` 준수 + 사이트맵 활용.
- **프리모아**: 목록 JSON(`/m4a/s41a`) 수집, **`client_id`(이메일)는 DB 저장 제외**, 개별 파트너 프로필(`/m7…`) 비수집.
- 공통: 과도 요청 금지, robots.txt 허용 범위 내, 개인정보 최소 수집.

### 3.3 포함/제외 요약
- **포함**: 5종 크롤러 수집·정제·DB 저장 / 소스별 목록·요약 / 시장조사 분석 / 기능 개선 분석 / 시장성 인사이트 / 단일 페이지 대시보드(차트·카드·표·추이) / 수집 이력·상태 표시 / 원본 URL 드릴다운.
- **제외(MVP)**: 로그인·권한, 상세 로그인 항목 크롤링, 추가 사이트(Phase 2), 실시간 수집, 입찰·매칭.

---

## 4. 기능 요구사항 & 우선순위 (M/S/C)

> 우선순위 표기: **M**=MVP 필수(Must) / **S**=Phase 2 선택(Should) / **C**=이후 고려(Could). requirements.md의 FR-ID와 일치.

| ID | 우선순위 | 기능 | MVP 포함 |
|---|---|---|---|
| FR-01 | **M** | 5종 크롤러(위시켓·프리모아·u300·u300 1기·Devpost) 안건 수집 | ✅ |
| FR-02 | **M** | 월 1회 자동 수집 배치(스케줄·수집 이력 기록) | ✅ (MVP: on-demand + 이력 기록, 자동 스케줄은 S) |
| FR-03 | **M** | 정제·표준화·중복 제거(공통 스키마, 단위 정규화) | ✅ |
| FR-04 | **M** | Neon(PostgreSQL) 저장·조회 | ✅ |
| FR-05 | **M** | 사이트별 수집 안건 목록/요약 | ✅ (목록 + 원본URL) |
| FR-06 | **M** | 시장조사 분석(수요·트렌드·분포) | ✅ |
| FR-07 | **M** | 기능 추가·개선 방향 분석 | ✅ |
| FR-08 | **M** | 시장성 인사이트 도출 | ✅ |
| FR-09 | **M** | 한눈에 보기 쉬운 대시보드 UI/UX(단일 페이지) | ✅ |
| FR-10 | **S** | 상세 드릴다운(개별 안건 상세/원문) | ⚠️ MVP는 **원본URL 링크 + DB 필드 기반 경량 상세**만 / 전체 상세(로그인 항목 포함)는 S |
| FR-11 | **S** | 수집 이력·실행 상태 표시 | ⚠️ MVP에 **최소 수집 이력**(최근 수집 시각·건수·실패) 포함 → S로 승격해 MVP에 넣음(SC-1 투명성) |

> 우선순위 판정 근거(요약): 수집→정제→분석→시각화의 데이터 파이프라인 4축이 사용자 성공 기준(SC-1 제대로 된 수집, SC-2 제대로 된 분석, SC-3 한눈에)의 전제이므로 FR-01~09를 전부 M으로 유지. FR-10은 "한눈에 보기"의 본질과 무관해 MVP는 경량(원본 링크)로, FR-11은 수집 신뢰 검증(SC-1)에 기여해 MVP 최소 표시.

---

## 5. 수용 기준 (Acceptance Criteria, Given/When/Then)

> 각 AC는 `test-design.md`에서 테스트 케이스로 매핑된다. Edge Case 포함.

### FR-01 — 5종 크롤러 수집
| AC-01 | Given/When/Then | Edge Case |
|---|---|---|
| AC-01-1 위시켓 도급 수집 | **Given** `crawl_wishket_oauth.py`가 쿠키·`pt=task_based`로 실행 가능한 상태<br>**When** 대시보드가 "위시켓 수집 실행"을 트리거<br>**Then** ① 목록 안건(제목·예산·기간·카테고리·레벨·지역·등록일·지원자)이 수집됨 ② 각 건이 `source='wishket'`로 DB에 적재됨 ③ 수집 건수·성공/실패가 `collection_runs`에 기록됨 | • 사이트 구조 변경→파싱 실패 시 오류 기록 후 중단(무한 재시도 금지)<br>• 로그인 쿠키 만료 시 명시적 오류<br>• crawl-delay 5 준수(요청 간 최소 5초 간격) |
| AC-01-2 프리모아 도급 수집 | **Given** `crawl_freemoa.py`가 쿠키·`workType=1`로 실행 가능한 상태<br>**When** 프리모아 수집 실행<br>**Then** ① 안건(제목·fld/카테고리·cost_min/max·during·지역·키워드·txt·workType) 수집 ② **`client_id`(이메일)는 저장 제외** ③ DB 적재·이력 기록 | • `txt`(상세) 파싱 실패 건은 txt=null로 적재(전체 실패 금지)<br>• 예산 단위는 만원→공통 단위 정규화 |
| AC-01-3 u300 현재 2기 수집 | **Given** `crawl_u300.py` 공개 REST API 사용<br>**When** u300 현재 수집 실행<br>**Then** ① 목록 357건+상세가 수집·적재 ② `source='u300'`, 런타입 `current` 태그 | • API 응답 code≠200이면 해당 건 스킵·오류 기록 |
| AC-01-4 u300 1기(과거) 수집 | **Given** `crawl_u300.py`(peNo) 사용<br>**When** u300 1기 수집 실행<br>**Then** ① 60건이 수집·적재 ② `runtime='past1'` 태그(라운드 진행 중, 수상 미발표) | • 이미 수집된 peNo는 중복 적재 금지(멱등) |
| AC-01-5 Devpost 수집 | **Given** `crawl_devpost.py` 공개 페이지 스크래핑<br>**When** Devpost 수집 실행<br>**Then** ① 소프트웨어 프로젝트 제목·설명·tech stack 수집·적재 ② `source='devpost'` | • 특정 상세 페이지 404 시 해당 건 스킵 |

### FR-02 — 월 1회 배치
| AC-02 | Given/When/Then | Edge Case |
|---|---|---|
| AC-02-1 자동 배치 | **Given** 월 1회 배치 스케줄이 설정됨(MVP on-demand + 수동 트리거 포함)<br>**When** 예정 시각/수동 트리거 도래<br>**Then** ① 5종 수집이 순차 실행 ② 각 소스별 결과(건수·성공/실패·소요시간)가 `collection_runs`에 기록 ③ 중복 실행 시 같은 날짜 런 중복 방지 | • 스케줄 실행 중 네트워크 장애→실패 상태 기록, 다음 주기에 재실행<br>• 수동 재실행은 멱등 |
| AC-02-2 수집 이력 조회 | **Given** 수집 런 기록이 존재<br>**When** 대시보드에서 수집 현황 보기<br>**Then** 최근 수집 시각·소스별 건수·상태(성공/부분/실패) 표시 | • 런 이력 없으면 "아직 수집 없음" 안내 |

### FR-03 — 정제·표준화·중복 제거
| AC-03 | Given/When/Then | Edge Case |
|---|---|---|
| AC-03-1 | **Given** 원본 수집 데이터가 존재<br>**When** 정제 파이프라인 실행<br>**Then** ① 공통 스키마(제목·카테고리·예산 min/max·기간일·지역·유형·기술키워드·등록일·원본URL·수집시각)로 매핑 ② 예산 단위 정규화(프리모아 만원→원/월, 위시켓 원/월 통일) ③ 동일 원본URL·ID 기준 중복 제거(멱등 upsert) ④ `client_id` 등 개인정보 필드 제외 | • 예산 "협의 후 결정"/"0"은 budget=null 처리<br>• 카테고리명 사이트별 상이→표준 카테고리 맵 필요<br>• 날짜 파싱 실패 시 registered_at=null 허용 |

### FR-04 — Neon 저장·조회
| AC-04 | Given/When/Then | Edge Case |
|---|---|---|
| AC-04-1 | **Given** Neon(PostgreSQL) 연결 정상<br>**When** 정제 데이터 적재·조회 API 호출<br>**Then** ① 표준 스키마 테이블에 적재 ② 대시보드 집계 쿼리로 조회 가능 ③ 스키마 마이그레이션 관리(버저닝) | • 커넥션 실패 시 배치 실패 처리·재시도<br>• 대량 적재 시 배치 insert/upsert |

### FR-05 — 사이트별 목록/요약
| AC-05 | Given/When/Then | Edge Case |
|---|---|---|
| AC-05-1 | **Given** 수집 데이터에서 특정 소스(예: 위시켓) 선택<br>**When** 소스별 안건 목록 요청<br>**Then** ① 소스 필터링된 안건 목록(제목·예산·기간·카테고리·등록일) 표시 ② 각 항목에 원본URL 링크 ③ 건수·요약 카드 노출 | • 필터 결과 0건 시 빈 상태 + 안내<br>• 페이징(대량) |
| AC-05-2 | **Given** 안건 목록 존재<br>**When** 항목 클릭<br>**Then** ① 경량 상세(DB 필드 기반) 표시 ② "원본 보기" 링크로 출처 사이트 이동(FR-10 경량) | • 원본 삭제/변경 시 링크 오류는 사용자 안내(외부 책임) |

### FR-06 — 시장조사 분석
| AC-06 | Given/When/Then | Edge Case |
|---|---|---|
| AC-06-1 카테고리 수요 점유율 | **Given** 정제 안건이 존재<br>**When** 대시보드 로드<br>**Then** 카테고리별 안건 수·점유율 차트 표시(전체+소스별) | • 미분류 카테고리는 "기타"로 집계 |
| AC-06-2 예산/기간 분포 | **Given** budget·period 값 존재<br>**When** 분포 차트 요청<br>**Then** 예산 구간 히스토그램·기간(일) 분포 표시(null 제외, 별도 카운트 표기) | • null 예산 건은 분포 제외+건수 별도 고지 |
| AC-06-3 상주vs도급 비중 | **Given** work_type 값 존재<br>**When** 비중 차트 요청<br>**Then** 도급/기간제/상주(및 해커톤·공모) 비중 표시 | • 값 없으면 "기타" |
| AC-06-4 기술 키워드 트렌드 | **Given** 기술키워드 필드 존재<br>**When** 키워드 랭킹 요청<br>**Then** 상위 키워드 빈도(전체+월간 변화) 표시 | • 키워드 없으면 공백 |
| AC-06-5 월간 등록 추이 | **Given** 등록일 데이터 존재<br>**When** 추이 차트 요청<br>**Then** 월별 신규 등록 건수 시계열 표시 | • 월별 데이터 부족 시 레이블 안내 |

### FR-07 — 기능 개선 방향 분석
| AC-07 | Given/When/Then | Edge Case |
|---|---|---|
| AC-07-1 | **Given** 수집 안건·공급 동향(지원자 수·레벨) 존재<br>**When** "기능 개선 방향" 섹션 요청<br>**Then** ① 수요가 많고 공급이 부족한 영역/요구 패턴(예: 공통 요구사항) 도출 ② 개선 후보 목록·근거(건수/추이) 표시 | • 분석 근거 데이터 부족 시 "분석 미실행" 안내(확정 수치 미표기) |

### FR-08 — 시장성 인사이트 도출
| AC-08 | Given/When/Then | Edge Case |
|---|---|---|
| AC-08-1 | **Given** 정제 안건·월간 추이 존재<br>**When** "시장성 인사이트" 섹션 요청<br>**Then** ① 고성장 카테고리/키워드(증가율 또는 건수 기반) 표시 ② 근거(수치·기간) 함께 표기 ③ **저신뢰 수치는 신뢰도 주석과 함께 표기, 정확한 수치 단정 금지** | • 증가율 계산 데이터 부족 시 계산 제외·주석 |
| AC-08-2 | **Given** 시장성 인사이트 존재<br>**When** 카드/표로 표시<br>**Then** 카테고리명·건수·추이(+/▲)·근거기간 표시 | • 단일 시점 데이터는 "추이 미확인" 주석 |

### FR-09 — 대시보드 UI/UX (한눈에)
| AC-09 | Given/When/Then | Edge Case |
|---|---|---|
| AC-09-1 | **Given** 대시보드 URL 접근<br>**When** 페이지 로드<br>**Then** ① 헤더 요약 카드(총 건수·최근 수집·소스별 건수) ② 소스별 현황 ③ 시장조사 분석(차트) ④ 기능 개선 방향 ⑤ 시장성 인사이트 ⑥ 수집 이력이 **한 화면에서 스크롤 없이 눈에 들어오는 레이아웃**으로 표시 | • 데이터 없으면 빈 상태 안내(로딩·에러 구분) |
| AC-09-2 | **Given** 대시보드 로드 완료<br>**When** 차트/카드 렌더링<br>**Then** 차트·카드·표가 반응형(모바일/데스크톱)으로 정상 표시 | • 짧은 데이터에서도 차트 깨짐 없음 |

### FR-10 — 상세 드릴다운 (S)
| AC-10 | Given/When/Then | Edge Case |
|---|---|---|
| AC-10-1 경량 상세 | **Given** 안건 항목 존재<br>**When** 안건 클릭<br>**Then** DB 필드 기반 상세 + 원본URL "원본 보기" 링크 표시(MVP) | • 필드 null은 "정보 없음" |
| AC-10-2 전체 상세 (S) | **Given** Phase 2 로그인 항목 포함 상세 데이터 존재<br>**When** 상세 열람<br>**Then** 모집 요건·근무 환경 등 상세 표시 | • 로그인 데이터 부재 시 안내 |

### FR-11 — 수집 이력·상태 (S, MVP 최소 포함)
| AC-11 | Given/When/Then | Edge Case |
|---|---|---|
| AC-11-1 | **Given** 수집 런 이력 존재<br>**When** 수집 상태 섹션 요청<br>**Then** 최근 수집 시각·소스별 건수·상태 표시 | • 이력 없으면 "아직 수집 없음" |
| AC-11-2 | **Given** 수집 부분 실패 존재<br>**When** 상태 확인<br>**Then** 실패 내역(건수·원인) 표시 | • 실패 원인 미기록 시 "원인 미상" |

---

## 6. MVP 범위 (Phase 1) / Phase 2 · Phase 3

### Phase 1 — MVP (필수)
- **데이터**: 위시켓·프리모아(도급), u300 현재+1기, Devpost — 5종 수집·정제·저장 (크롤러 이미 준비)
- **기능**: FR-01~09 전부 + FR-10 경량(원본링크) + FR-11 최소 수집 이력
- **대시보드**: 단일 페이지 — 헤더 요약 카드 / 소스별 현황 / 시장조사 분석(카테고리·예산·기간·상주·키워드·월간추이 차트) / 기능 개선 방향 / 시장성 인사이트 / 수집 이력
- **분석 로직**: 정량(분포·추이·점유율) 규칙 기반 + 정성 인사이트(수동/규칙 템플릿). 저신뢰 수치 주석.
- **기술**: Cloudflare(Workers/Pages) + Neon(PostgreSQL) + Rust(API/분석 보조) + Python(크롤러·분석). 상세는 trd.md.

### Phase 2 (Should)
- FR-10 전체 상세(로그인 항목 포함, 선택적 수집), FR-11 고급(수집 실패 원인·대시보드 고도화)
- 추가 크롤러: 크몽·Freemo·CyanNest·아임잡 (robots 허용 확인됨 / 원티드긱스 제외)
- 월 1회 **완전 자동** 배치(스케줄러 고도화, 알림)
- 자동 수집 스케줄러 무인 실행 정착

### Phase 3 (Could)
- 시장성 점수(복합 지표) 자동 산출 고도화, 비교 뷰(소스 간·기간 간), 데이터 내보내기(CSV), 알림/구독, 역사적 데이터 누적 장기 추이

---

## 7. 리스크 · 가정 · 오픈 이슈

### 7.1 리스크
| ID | 리스크 | 영향 | 완화 |
|---|---|---|---|
| R1 | 사이트 구조/API 변경으로 크롤링 실패 | 수집 중단(SC-1) | 오류 기록·부분 실패 처리, 재실행 멱등, 구조 변경 대응 분리 |
| R2 | 로그인 쿠키 만료(위시켓·프리모아 크롤러) | 수집 공백 | 쿠키 재발급 프로세스, 오류 명시 |
| R3 | robots/이용약관 위반·과도 요청 | 법적·차단 | crawl-delay 준수, robots 준수, 개인정보(이메일) 제외, 요청 최소화 |
| R4 | 데이터 단위/카테고리 표준화 오류 | 분석 왜곡 | 단위 정규화 맵, 표준 카테고리 맵, null 처리 명시 |
| R5 | 시장성 수치 신뢰도(마케팅 수치·2차 집계 상충) | 오판단 | 원천 크롤 값 우선, 저신뢰 주석, 단정 금지(리서치 §5) |
| R6 | MVP 크로스-출처 통합(도급/공모/해커톤 이질성) 분석 복잡 | 분석 범위 산만 | 소스별·그룹별(SI도급/공모/해커톤) 분리 집계, 공통 스키마 그래프 수준 통합 |

### 7.2 가정 (Assumptions)
- A1 크롤러는 Python, 백엔드·API·배포는 Cloudflare(+Rust), DB는 Neon (trd.md에서 확정).
- A2 "한눈에" = 단일 웹 페이지의 시각적 요약(차트·카드·표).
- A3 수집 주기는 월 1회; MVP는 on-demand + 이력, 자동 스케줄은 Phase 2.
- A4 분석 = 정량(분포·추이) + 정성(기능 방향), 수치 근거 우선.
- A5 크롤러 실행 환경(`curl_cffi` venv vs `lzstring` venv 분리)은 trd.md에서 격리 설계.

### 7.3 오픈 이슈 (기획 단계 결정→이관)
| ID | 이슈 | 결정/이관 |
|---|---|---|
| OQ-1 | 수집 소스 확정 | **결정**: MVP 5종(위시켓·프리모아·u300·u300 1기·Devpost), Phase2에 크몽 등 |
| OQ-2 | 위시켓 상세 로그인 항목 | **결정**: 목록 필드 기반(MVP). Phase2 검토 |
| OQ-3 | FR-10 드릴다운 | **결정**: MVP 경량(DB필드+원본링크) |
| OQ-4 | 분석 지표 | **결정**: 카테고리 점유율·예산/기간 분포·상주vs도급·키워드 트렌드·월간 추이·기능방향·시장성 |
| OQ-5 | 수집 주기·스케줄러·실행환경 | **이관(개발팀)**: myVp on-demand+이력, 자동 스케줄 Phase2, 크롤러 venv 격리(trd.md) |
| OQ-6 | Devpost 수집 범위(전체 vs 샘플)·해커톤 분석 깊이 | MVP 샘플+기능확인, Phase2 전수 |

---

## 8. 성공 지표 (KPIs, 데이터 기반 / 사용자 성공기준 연계)

| KPI | 정의 | 목표(MVP) |
|---|---|---|
| 수집 완류율 (SC-1) | 소스별 수집 성공 건수/예상 건수 | 각 소스 100% 시도, 실패 내역 명확 기록(무중복) |
| 중복률 (SC-1) | 중복 적재 비율 | 0%(멱등 upsert) |
| 분석 지표 산출 (SC-2) | 시장조사·기능방향·시장성 인사이트 섹션 산출 여부 | 5종 데이터로 모든 지표 섹션 정상 표시 |
| 대시보드 가용 (SC-3·SC-4) | URL 접근·한 화면 요약 | 로그인 없이 접근, 로드 정상 |
| 데이터 신뢰 (SC-2) | 저신뢰 수치 주석 표기 준수 | 주석 없이 단정 금지 |

---

## 9. 다음 단계 인계 (디자인·개발)

- **디자인팀**: `userflow.md`(진입→소스별→분석→시장성) 기반 단일 페이지 스토리보드. 카드·차트·표 레이아웃, 빈 상태·에러 상태, 반응형, 저신뢰 수치 주석 표기(시장성 인사이트).
- **개발팀**: `trd.md`(스택·크롤러 실행환경 격리)·`tasks.md`·`test-design.md` 기반 설계·TDD. 크롤러 venv 분리, `client_id` 제외, 멱등 적재, crawl-delay 준수.
- **QA(기획QA·개발QA)**: `test-design.md` AC→TC 매핑으로 정합성·수용 기준 검증.

---
*끝 — 본 문서는 requirements.md를 대체하지 않으며, 하위 단계(디자인·개발·QA)의 기준 문서다.*
