# 테스트 설계 (test-design.md) — 시장조사·분석 대시보드

- **작성팀**: 기획팀 (planning-team) · **모드**: standard
- **기준**: plan.md AC(§5), trd.md, requirements.md 수용기준·SC
- **사용 스킬 로그**: `plan-tests`(AC→테스트 레벨/케이스 매핑 구조), `test-case-writer`(정상·경계·예외 케이스), `test-strategy`(테스트 레벨), `analyze-requirements-plus`(테스트 포인트·경계)
- **레벨 표기**: U=단위 / I=통합 / E=End-to-End(UI) / C=계약(API)

---

## 1. 테스트 전략 요약

- **레벨**: 크롤러(단위+통합) → 정제/적재(통합) → 분석(단위/통합) → API(계약) → 대시보드(End-to-End).
- **데이터**: 실제 크롤 샘플(`crawled/`) 활용 + 소스별 최소 데이터셋 시드.
- **초점(성공기준 SC)**: SC-1 수집 무결성(누락·중복·실패 기록), SC-2 분석 산출, SC-3 한눈에 보기, SC-4 로그인 없이 접근.
- **도구**: pytest(크롤러·정제·분석), API 계약 테스트(웹 요청), Playwright/웹 E2E(대시보드). 저신뢰 수치·null·경계값 중점.

---

## 2. AC → 테스트 케이스 매핑

| AC | 테스트 | 레벨 | 시나리오 | 기대 결과 |
|---|---|---|---|---|
| **AC-01-1** | TC-01 크롤러 위시켓 정상 수집 | U/I | 위시켓 목록 수집 | 목록 필드 수집·DB `source='wishket'` 적재, 이력 기록 |
| | TC-02 위시켓 crawl-delay 준수 | I | 요청 간격 검증 | 5초 이상 간격(요청 로그) |
| | TC-03 사이트 구조 변경/파싱 실패 | U | 응답 형식 불일치 주입 | 오류 기록·중단(무한 재시도 없음), 부분 실패 처리 |
| | TC-04 쿠키 만료 | U | 401/만료 응답 | 명시적 오류 상태 |
| **AC-01-2** | TC-05 프리모아 정상 수집 | U/I | `/m4a/s41a` 수집 | 필드·txt 수집, DB 적재 |
| | TC-06 **`client_id` 저장 제외** | I | 수집 응답에 이메일 포함 | 적재 데이터에 이메일 컬럼/값 없음(검증) |
| | TC-07 예산 단위 정규화 | U | 만원→원/월 | 단위 통일, null 정책 적용 |
| **AC-01-3** | TC-08 u300 현재 수집 | I | 목록357+상세 | `source='u300'`, `runtime='current'` 적재 |
| **AC-01-4** | TC-09 u300 1기 수집 | I | peNo 60건 | `runtime='past1'` 적재, 재실행 멱등 |
| **AC-01-5** | TC-10 Devpost 수집 | U/I | 상세 페이지 스크래핑 | 제목·설명·스택 적재 `source='devpost'` |
| | TC-11 Devpost 상세 404 | U | 404 페이지 | 해당 건 스킵·오류 기록 |
| **AC-02-1** | TC-12 월간 배치·중복 실행 방지 | I/E | 같은 날짜 런 재실행 | 2차 런 중복 방지(멱등), 런 기록 |
| | TC-13 네트워크 장애 처리 | I | 실패 주입 | 실패 상태 기록, 재실행 가능 |
| **AC-02-2** | TC-14 수집 이력 조회 | E | `/api/runs` 요청 | 최근 시각·소스별 건수·상태 표시; 이력 없으면 안내 |
| **AC-03-1** | TC-15 중복 제거(멱등) | I | 동일 원본URL·source 재적재 | 중복 0(upsert) |
| | TC-16 null·경계(예산 "협의 후", 0) | I | null/0 데이터 | budget=null 처리·분포 제외+카운트 |
| | TC-17 카테고리 표준화 | I | 사이트별 상이 카테고리 | 표준 맵 매핑, 미분류="기타" |
| **AC-04-1** | TC-18 Neon 적재·조회 | I | 정제 데이터 적재·조회 | 스키마 적재, 조회 쿼리 정상 |
| | TC-19 커넥션 실패 | I | 커넥션 차단 | 배치 실패 처리·재시도 |
| **AC-05-1** | TC-20 소스 필터 목록 | E | 소스별 목록·페이징 | 정상 표시, 빈 상태·0건 안내 |
| **AC-05-2** | TC-21 항목 클릭→경량 상세·원본 | E | 안건 클릭 | DB 상세+원본URL 링크 |
| **AC-06-1** | TC-22 카테고리 점유율 | I/E | 집계 API | 카테고리별 건수·점유율, 기타 집계 |
| **AC-06-2** | TC-23 예산/기간 분포(null) | I/E | null 포함 데이터 | 히스토그램·기간 분포, null 제외+표기 |
| **AC-06-3** | TC-24 상주vs도급 비중 | I/E | 유형 집계 | 비중 표시, 값 없으면 기타 |
| **AC-06-4** | TC-25 키워드 랭킹·트렌드 | I/E | 키워드 집계 | 상위 키워드·월간 변화 |
| **AC-06-5** | TC-26 월간 등록 추이 | I/E | 등록일 시계열 | 월별 건수 시계열, 부족 시 안내 |
| **AC-07-1** | TC-27 기능 개선 방향 | I/E | 공급/수요 분석 | 후보 목록+근거(건수); 근거 부족 시 "분석 미실행" |
| **AC-08-1** | TC-28 시장성 인사이트 | I/E | 성장률 계산 | 고성장 카테고리/키워드+추이+근거기간 |
| | TC-29 저신뢰 수치 주석 | I/E | 저신뢰 데이터 | 신뢰 뱃지 표기·단정 금지(미표기 시 실패) |
| **AC-09-1** | TC-30 단일 페이지 6섹션 | E | 대시보드 로드 | 헤더·소스·분석·기능·시장성·이력 한 화면 표시 |
| **AC-09-2** | TC-31 반응형·차트 렌더 | E | 모바일/데스크톱 | 차트·카드·표 정상, 짧은 데이터에서도 깨짐 없음 |
| **AC-10-1** | TC-32 경량 상세·원본(경계) | E | null 필드 | "정보 없음" 표시 |
| **AC-11-1** | TC-33 수집 이력 상태 | E | 런 이력 조회 | 최근 시각·건수·상태; 없으면 "아직 수집 없음" |
| **AC-11-2** | TC-34 부분 실패 표시 | E | 부분 실패 런 | 실패 건수·원인 표시(원인 미기록 시 "원인 미상") |

---

## 3. 테스트 케이스 상세 (주요 골격)

### TC-06 개인정보 제외 (프리모아 `client_id`) — **중요**
- **Given** 프리모아 수집 응답(목록 JSON)에 `client_id`(이메일)이 포함
- **When** 정제·적재 파이프라인 실행 후 `projects` 조회
- **Then** ① 적재 컬럼에 이메일 값 없음 ② raw_json에도 `client_id` 미포함 ③ 대시보드 어디에도 이메일 미노출
- **경계**: `client_id`가 비이메일 토큰(예: `lduckh`)인 경우도 포함·제외 여부를 컬럼 명세로 결정(저장 제외 원칙).

### TC-15 중복 제거 (멱등)
- **Given** 동일 원본URL·source의 안건이 이미 있고 동일 런 재실행
- **When** 재적재
- **Then** 같은 (source, source_ref) 행이 새로 추가되지 않음(upsert), 건수 유지.

### TC-29 저신뢰 수치 단정 금지
- **Given** 시장성 인사이트에 신뢰도 낮음(low) 항목 존재(예: 마케팅성 수치)
- **When** 인사이트 표시
- **Then** ① 신뢰 뱃지(낮음) 표시 ② "~로 추정/공지", 단정 표현(값 절대 확정) 없음 ③ 정확한 확정 수치 문구 검출 시 실패.

### TC-31 반응형·차트
- **Given** 다양한 뷰포트(모바일 375px / 데스크톱 1280px) + 소량 데이터
- **When** 대시보드 렌더
- **Then** 차트·카드·표가 넘침 없이 정상 표시, 스크롤/터치 가능.

---

## 4. 회귀·비기능 테스트 포인트

| 영역 | 테스트 | 레벨 |
|---|---|---|
| NFR-02 성능 | 대시보드 로드·API 응답 임계(캐시 히트 <200ms) | E |
| NFR-05 견고성 | 크롤러 실패 시 무한 재시도 없음·오류 기록, 부분 실패 | I |
| NFR-06 보안 | 개인정보(이메일) 미저장·미노출, DB 접근 최소 권한, 평문 DSN 없음 | I/E |
| 데이터 무결성 | 원본URL·source 유니크, 중복 0, 날짜/예산 정규화 | I |

---

## 5. 릴리스 게이트 (Pass 조건)

- P0(차단): TC-06(개인정보), TC-15(중복), TC-29(저신뢰 단정) + 수집·적재·대시보드 로드 정상.
- P1(필수): 전체 AC 매핑 테스트 통과(AC-01~11).
- P2(권장): 성능·반응형·빈 상태.
- 수용 기준(requirements SC-1~4) 모두 충족 시 승인.

*끝 — 개발QA팀이 실시하고 dev-qa-report.md로 보고한다.*
